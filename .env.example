# Gummy never ships model secrets into browser JavaScript.
# Point the shell at a trusted local or server-side model broker instead.
GUMMY_MODEL_BROKER_URL=http://localhost:8787
GUMMY_MODEL_PROVIDER=demo
