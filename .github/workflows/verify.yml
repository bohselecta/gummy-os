name: verify

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  standalone:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Run complete verification and retain log
        shell: bash
        run: |
          mkdir -p artifacts
          set -o pipefail
          npm run verify 2>&1 | tee artifacts/verify.log
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: standalone-founder-review
          path: |
            artifacts/verify.log
            artifacts/playwright-report
            artifacts/test-results
            artifacts/evidence
            artifacts/product-preservation-report.json
            examples/standalone-acceptance.return.json
