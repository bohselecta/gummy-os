# Codex One-Turn Final Execution Prompt

Copy the complete prompt below into Codex from a workspace that can access the four canonical repositories.

---

## BEGIN CODEX PROMPT

You are completing the founder-ready personal release of Gummy OS.

This is an implementation and release turn, not another architecture, audit-only, or proposal turn.

### Founder authorization

Hayden Lindley has authorized you to:

- inspect the exact current state of the canonical repositories;
- reconcile the accepted contracts;
- create or reuse one final release branch per repository;
- implement every remaining phase in the controlling finish-up kit;
- add schemas, migrations, adapters, UI, copy, tests, evidence, and documentation;
- perform ordinary repository and PR housekeeping;
- commit and push phase checkpoints;
- open final pull requests;
- merge additive/backward-compatible specialist prerequisites after all applicable gates pass;
- merge the final Gummy OS release PR only when every required final gate is PASS.

You are not authorized to:

- bypass Human authority, rights, privacy, cost, security, or evidence boundaries;
- fake a live provider/runtime result;
- weaken or skip tests merely to obtain green status;
- overwrite source or accepted Assets;
- flatten ImageHoss, VideoBoss, or Meshmallow into generic Gummy OS forms;
- expose provider secrets in browser code;
- widen local bridges into arbitrary execution APIs;
- discard historical 3D Bee identity/evidence;
- claim founder-ready while a required gate is BLOCKED or FAIL.

### Canonical repositories

```text
bohselecta/gummy-os
bohselecta/imagehoss
bohselecta/videoboss
bohselecta/3d-bee
```

Start by resolving the actual default-branch heads. The finish kit was written from these observed heads, but current GitHub state is authoritative:

```text
gummy-os   c46e5f4a0719b06a7dcdfd815bb8f3d5a89daa62
imagehoss  1c54a527ec290d6da4886a80474aad40c3433fff
videoboss  cea920cb254194fa9350bb1a04ebc2e88030ca49
3d-bee     4b56cf01824f0db27b86800e547b3fefc168c407
```

If a default branch moved, inspect and reconcile the new commits before editing. Do not reset or discard newer work.

### Mandatory read order

Read these files from `bohselecta/gummy-os` in full before editing code:

1. `docs/finish-kit/README.md`
2. `docs/finish-kit/MASTER_RELEASE_ROADMAP.md`
3. `docs/finish-kit/PRODUCT_READINESS_AND_UX.md`
4. `docs/finish-kit/SPECIALIST_INTEGRATION_BLUEPRINT.md`
5. `docs/finish-kit/INFRASTRUCTURE_AND_GUMMY_BOX.md`
6. `docs/finish-kit/ACCEPTANCE_AND_RELEASE_MATRIX.md`
7. `docs/finish-kit/RISKS_STOP_RULES_AND_DEFERRED_SCOPE.md`
8. `docs/finish-kit/release-program.json`
9. `docs/CREATIVE_SPECIALIST_PRODUCTION_CONTRACTS.md`
10. `docs/IMAGEHOSS_PRODUCTION_PROMPT_CONTRACT_WORKSHEET.md`
11. `docs/ACTOR_HOME_PRODUCTION_UNDERGROUND_CENTER.md`
12. `docs/AUTOMATED_ACCEPTANCE.md`
13. `docs/FULL_PRODUCT_PRESERVATION_DIRECTIVE.md`
14. `docs/BRAND_SYSTEM.md`
15. `docs/BRAND_ASSET_CATALOG.md`
16. `docs/ACTOR_FIRST_PRODUCTION_MODEL.md`
17. `docs/PRODUCTION_ACTOR_RUNTIME.md`
18. `docs/ACTOR_AGENT_MASTER_CONTROL.md`
19. `docs/GUMMY_BOX_WORK_ORDERS.md`
20. `AGENTS.md`

Then read the current README, package scripts, canon, protocols, phase receipts, and source seams in each specialist repository.

### Precedence

For the remainder of this release:

```text
finish-up kit
→ accepted founder/product contracts
→ current canonical code and schemas
→ older active plans
→ historical receipts and proposals
```

Earlier instructions that said to stop after an ImageHoss contract proposal, a vertical-slice recommendation, a gap audit, or an intermediate founder review are superseded. Reconcile the contract, implement it, test it, commit it, and continue.

Human authority, brand locks, rights, privacy, source immutability, Make Production execution semantics, specialist identity, and evidence rules are not superseded.

### Operating mode

Work through Phases 0–9 in `MASTER_RELEASE_ROADMAP.md` in order.

This is one sustained turn. Do not ask for confirmation between phases. Do not stop after returning a plan. Do not send Hayden ordinary manual regression instructions.

At every phase:

```text
inspect exact state
→ run baseline/applicable tests
→ implement the complete bounded phase
→ add/update automated acceptance
→ capture evidence and limitations
→ commit checkpoint
→ continue
```

Use one final branch per touched repository:

```text
codex/final-release-2026-07-27
```

If a compatible final-release branch already exists, reconcile it rather than creating a competitor.

Do not create a large fan-out of tiny PRs. Use checkpoint commits and one final PR per repository.

### Required product outcome

The release must let a clean user:

```text
open Gummy OS
→ understand it within seconds
→ start locally without an account
→ open a blank or Night Gummy Launch sample Production
→ add/configure specialist Actors without execution
→ inspect locks, references, route, cost, locality, rights, outputs, and limitations
→ choose Make Production
→ freeze one immutable Run
→ create Work Orders, Context Envelopes, Molds, Leases, and Grants
→ execute eligible real or deterministic routes truthfully
→ recover/cancel without unsafe duplication
→ compare results
→ Human-accept role-specific outputs
→ create downstream handoffs
→ inspect Returns and linked specialist/Gummy OS Receipts
→ close and return to intact state
→ export and restore a complete Local Gummy Box backup
```

### Architecture freeze

Do not reopen the product ontology.

Preserve:

- Gummy OS, Gummy Canvas, Gummy Bar, Gummy, Glopper, Gummy Box;
- Human, Actor, Agent, Mold, Master Control;
- Production, Context Envelope, Run, Work Order, Task Lease, Capability Grant, Return, Receipt;
- Night Gummy and Day Gummy only;
- exact five-color brand anchors;
- Local Box without an account;
- separate specialist repositories and native evidence;
- `Make Production` as the sole Production execution transition;
- Human acceptance as final and role-specific;
- Production choices isolated from Actor memory unless an explicit ActorUpdateProposal is approved.

### Specialist requirements

#### ImageHoss

Do not stop at type mapping.

Implement the full Production-scoped ImageHoss adapter and product-native configuration:

- accepted complete relationship: Direction → References → Route → Generate → Compare → Accept → Handoff → Receipt;
- deterministic provider-neutral Prompt Package;
- no model call during configuration;
- versioned compatible migration from current R1–R5 types and `.hoss` bundles;
- authenticated loopback capability discovery;
- Make Production authority intersection;
- one owned native Job;
- recovery and cancellation;
- original/proxy import and exact hashes;
- comparison and Human acceptance;
- `.hoss` preservation;
- VideoBoss handoff;
- linked native/platform Receipts;
- cross-Production isolation and restart persistence.

When a supported local runtime exists, run one genuine smoke and prove `simulation: false`. When absent, finish the adapter/tests/unavailable state/live-smoke command, mark live execution NOT CLAIMED, and continue.

#### VideoBoss

Preserve planner, board, router, Cost Shield, reviewer, continuity, exports, and simulator.

Implement:

- Production-scoped direction/continuity/sequence/shot/take packages;
- deterministic configuration preview without render;
- accepted ImageHoss Asset intake;
- one trusted server-side real-provider broker seam selected from actual current environment suitability;
- provider keys outside browser;
- idempotency, cost ceiling, progress, recovery, cancellation, result hash;
- take review, comparison, Human acceptance, delivery;
- ImageHoss repair and Meshmallow spatial handoffs;
- linked native/platform Receipts.

If no provider credential exists, complete all adapter/broker/mock/E2E/live-smoke work and mark live rendering NOT CLAIMED. Do not claim a simulated take as real.

#### Meshmallow / `bohselecta/3d-bee`

Use Meshmallow publicly while preserving repository, `app:3d-bee`, `actor:3d-bee`, old protocols, hashes, and Receipts.

Implement:

- Production-scoped world intent, references, locks, exploration, Scene Plan, output/engine contract, acceptance;
- deterministic package preview without Blender execution;
- exact plan digest and Human approval;
- Work Order/Context/Mold/Lease/Grant mapping;
- authenticated short-lived supervisor session;
- reviewed named operations only;
- recovery/cancellation;
- checkpoints, preview, editable `.blend`, supported export, manifest validation when genuinely available;
- linked native/platform Receipts;
- legacy identity migration/aliases.

Never add arbitrary Python, shell, filesystem browsing, manufacturing claims, or finished-game claims.

When supported Blender exists, run one genuine small-scene proof. When absent, complete the adapter/mock/fail-closed/live-smoke work, mark live artifact NOT CLAIMED, and continue.

### Gummy Box requirements

- Local Box exists automatically;
- IndexedDB/OPFS remain authoritative for local records/bytes;
- no account or GitHub setup is required;
- implement inspect-first versioned backup export/import;
- prove clean-context restore with identical IDs/hashes/relationships/evidence;
- implement recovery and scoped reset;
- treat GitHub, Drive, and Managed Box as optional connections;
- normal UI must never ask for GitHub App IDs, PEM keys, installation IDs, buckets, or Vercel environment variables;
- external authority/sync changes require explicit Human choice and Receipt;
- missing optional connector must not block the local release.

### UX and copy requirements

Implement `PRODUCT_READINESS_AND_UX.md`, not a generic interpretation.

At minimum:

- five-second comprehension;
- Local Box ready copy;
- blank + Night Gummy Launch entry;
- progressive disclosure of protocol detail;
- structured specialist surfaces rather than generic key/value forms;
- complete empty/loading/unavailable/blocked/denied/failed/cancelled/recovery/completed/acceptance copy;
- actual next actions;
- Gummy orientation voice and Glopper action voice;
- public Meshmallow naming;
- no Ranch Day-only product doorway;
- no stale narrow `reference-preparation` public definition;
- no false live capability claim.

### Testing and evidence

Implement and satisfy `ACCEPTANCE_AND_RELEASE_MATRIX.md`.

The computer tests the computer. Hayden is not the test harness.

Do not disable, skip, loosen, or update visual baselines merely to obtain green status. Every feature requires unit/integration/browser evidence at the correct layer. Every live claim requires live evidence. Every external blocker receives an exact status and user-facing truth.

Create the required evidence files, including:

```text
evidence/final-release-baseline.json
evidence/product-readiness-audit.json
evidence/product-copy-audit.json
evidence/final-acceptance-summary.json
evidence/final-release-manifest.json
evidence/performance-budget.json
evidence/rollback-plan.md
```

Keep large binary evidence in appropriate CI artifacts/specialist repositories and link it exactly.

### Stop rules

Read and obey `RISKS_STOP_RULES_AND_DEFERRED_SCOPE.md`.

A hard stop applies to the affected unsafe mutation, not automatically to the whole turn.

Missing provider credentials, ComfyUI, Blender, a managed Box, Drive, Gummy Rooms, or long-horizon social/enterprise capabilities do not stop unaffected phases. Finish the code, deterministic proof, truthful unavailable state, and live-smoke command, then continue.

Stop the overall release merge when a required deterministic gate is FAIL or BLOCKED, data may be lost, authority can be bypassed, source immutability fails, capability truth is ambiguous, or rollback is impossible.

### PR and merge order

After all implementation and evidence are pushed:

1. open final ready PRs for ImageHoss, VideoBoss, and 3D Bee/Meshmallow;
2. cross-link exact heads and Gummy OS dependency fixtures;
3. merge additive/backward-compatible specialist PRs only when applicable deterministic gates pass and old fixtures remain compatible;
4. rebase/reconcile the Gummy OS branch onto the resulting exact specialist heads/contracts;
5. run full production-like Gummy OS acceptance;
6. open the final ready Gummy OS PR;
7. merge it only when every required release gate is PASS.

If a final required gate is BLOCKED or FAIL, leave the fully implemented PR open and return the exact blocker. Do not discard or hide completed work.

### Final Return

Return one release report, not a new plan.

Include:

1. exact repository, branch, head, PR, and merge status for all four repositories;
2. work completed by Phase 0–9;
3. files/contracts/migrations added;
4. test commands, totals, status, and environments;
5. preview/production URLs and exact deployed commit;
6. deterministic golden Production evidence;
7. live ImageHoss/VideoBoss/Meshmallow status with PASS, BLOCKED, or NOT CLAIMED;
8. Local Box backup/restore evidence;
9. accessibility, visual, security, reliability, and performance status;
10. migration and rollback anchors;
11. known limitations and exact external blockers;
12. final release level;
13. at most one genuine founder decision that automation cannot make.

Do not give Hayden a routine test checklist. If the final release is founder-ready, the final instruction is:

> Open this exact release. Start locally or open Night Gummy Launch. Begin using Gummy OS.

Begin now by reading the finish kit and resolving the exact repository heads. Continue through the complete program in this turn.

## END CODEX PROMPT
