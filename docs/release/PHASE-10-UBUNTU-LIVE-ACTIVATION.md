# Phase 10 Ubuntu live activation

Date: 2026-07-27

Reconciled specialist prerequisite heads:

- ImageHoss `340f819b20c5b6d7ea988459c9380759941c757f`;
- VideoBoss `e67db769219e5a764821f7bac74638f3791dca98`;
- Meshmallow/3D Bee `0c911f7552739f2e0bdefaf863a78a53f04a04c0`.

The Meshmallow GitHub Actions jobs did not start because the account Actions
budget prevented runner assignment. That is retained as an external CI
limitation, not represented as a passing hosted run. The exact merged head
passed the full local 37-Rust/21-Python/contracts/build/audit gate before merge.

`makeProduction()` now freezes and dispatches each execution node through its
selected route:

- deterministic configurations use the existing browser reference Agent;
- ImageHoss live configurations use the paired local adapter;
- VideoBoss live configurations require a trusted server adapter and an
  accepted, rights-cleared same-Production ImageHoss original;
- Meshmallow live configurations require an authenticated Blender supervisor.

Preview/configuration remains non-executing. Missing live capability blocks
only that node, never substitutes simulation, and writes a Return plus Receipt.
Mixed Runs retain per-node route/status, native Job IDs, specialist Job
evidence, separate specialist/Gummy Receipts, locality, cost, and immutable
source revisions. Ambiguous native calls become recovery-required and block
future submission pending inspection.

The production browser at `127.0.0.1:4173` completed an exact-origin CORS/CSP
probe through the ImageHoss bridge at `127.0.0.1:5214`: ephemeral pairing
returned 201 with `persisted: false`, and authenticated capability discovery
returned ready with one reviewed model. Provider credentials and native
Blender authority remain absent from the browser bundle.

Verification passes 95 Node tests, schema/product/brand/security gates,
production build, 23 Playwright tests, accessibility/visual/performance
budgets, and full-product preservation. New dispatcher regressions prove:

- no Job during preview;
- exact deterministic/live route freeze;
- no live-to-simulation fallback;
- node-local fail-closed behavior with mixed status;
- linked but distinct specialist and Gummy evidence;
- preserved native ID and inspect-first ambiguity recovery;
- paid VideoBoss blocked before discovery/submission without accepted source.

Hayden accepted the genuine ImageHoss candidate for `production-image` and
`video-first-frame`, including its disclosed two-character/no-safe-area
variance, and accepted the hash-bound Meshmallow scene for `accepted-scene`
and `engine-handoff`.

The accepted image then drove a genuine VideoBoss fal.ai Job through the exact
Wan 2.7 route. A first request was terminally rejected before inference because
duration was encoded as string `"5"`; it was not automatically retried. The
one explicitly authorized technical-defect replacement sent integer `5` and
succeeded as request `019fa4f4-25ba-7193-ba4f-739bc9b87f4f`, producing a
1280×720, 30 fps, 150-frame result with SHA-256
`db8811fcb7cd8e78c21a74f9e8d28daf6bcf7f424059aa1ded6b0c93c34514d6`
at recorded cost `$0.50`. Because the provider container included an audio
stream even though no audio input was supplied, the immutable original is
retained and an H.264 stream-copy delivery candidate removes audio without
re-encoding. Its SHA-256 is
`391a41f2a34a9bd8e5dbb2b8deb4f8b8633ad317860af2c0ef03770b79f2f247`.
Provider completion remains distinct from Human take acceptance.
